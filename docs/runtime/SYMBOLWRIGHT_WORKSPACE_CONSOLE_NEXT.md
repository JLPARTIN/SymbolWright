# Workspace Console Next

CM-200-N publishes the workspace entrypoint through package metadata after CM-200-M added the executable source.

The follow-up bundle should wire `codemind workspace [mission]` into the primary CLI router after the package bin path is validated.
