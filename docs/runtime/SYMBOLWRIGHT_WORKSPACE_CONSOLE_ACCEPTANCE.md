# CodeMind Workspace Console Acceptance

CM-200-L is ready when CI proves the workspace render layer compiles, renderer tests pass, command adapter tests pass, lint remains clean, and build remains green.

This bundle keeps top-level bin routing for the follow-up bundle after the render layer is validated.
