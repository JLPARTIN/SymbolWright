# README Ajna docs command sync

This note records the README alignment bundle for the Ajna docs command.

## Updated references

- `symbolwright ajna docs` is listed in the README CLI surface.
- `symbolwright ajna docs` is included with the local fixture pipeline helper commands.
- The current foundation docs list includes the Ajna docs command page and recent client collector command docs.

## Boundary

This bundle is documentation-only.

It does not add live GitHub ingestion, provider calls, shell execution, PR comments, approval actions, merge actions, or repository mutation behavior.
