# PR-CM-200-L Scope Lock

This PR bundle is locked to the deterministic workspace console render layer and command adapter.

Do not add provider calls, shell execution, GitHub writes, approval bypasses, or top-level bin routing in this bundle.
